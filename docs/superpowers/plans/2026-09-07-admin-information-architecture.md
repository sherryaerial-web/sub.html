# 管理者工作台資訊架構實作計畫

## 1. 鎖定前端契約

- 新增管理分區與管理首頁契約測試。
- 確認 15 個既有 `data-admin-tab`、既有按鈕 ID 與老師端導覽仍存在。
- 先執行聚焦測試並確認新契約失敗。

## 2. 建立管理分區與首頁

- 新增七個分區入口及分區對應表。
- 新增今天待辦、本月流程與常用操作的渲染。
- 保留既有頁籤作為分區內次導覽，支援既有網址與提醒跳轉。

## 3. 拆分營運與通知

- 月度營運流程顯示於每月營運分區。
- 通知中心只顯示手動推播、排程、範本與紀錄。
- 更多工具集中顯示既有同步、核對、請假開關、代老師操作與匯出。

## 4. 響應式與發布驗證

- 完成桌面雙欄與手機水平分區列樣式。
- 執行聚焦測試、完整測試與視覺檢查。
- 檢查差異後提交功能分支，合併至 `main`，推送 GitHub，執行 `clasp push --force` 並更新正式 GAS deployment。
- 不執行任何正式 Sheet 寫入或資料遷移。
